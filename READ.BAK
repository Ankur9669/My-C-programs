#include<ankur.h>
#include<stdlib.h>
#include<fstream.h>
#include<string.h>
void main()
{
	clrscr();
	FILE *fp = fopen("B:\\abcd.txt", "w");
	char ch[100];
	printf("Enter The String");
	scanf("%s", ch);
	fprintf(fp, "%s", ch);
	fclose(fp);
	getch();
}

