#include<ankur.h>
#include<stdlib.h>
void main()
{
	int i;
	int integer;
	int rem;
	int sum = 0;
	int k = 0;
	int ans[15];
	int temp;
	title();
	printf(" Enter the number: ");
	scanf("%d", &integer);
	for ( i = 1; i <= integer; i++)
	{
	 sum = 0;
	 while (integer > 0)
	 {
	   temp = integer;
	   rem = integer % 10;
	   if (j == 5 || j == 2)
	   {
	     sum = sum + rem;
	     if (sum == 4 || sum == 7 || sum == 2 || sum == 5)
	     {
	      ans[k] = temp;
	      k++;
	     }
	   }
	   integer = integer / 10;
	 }
	}
	for ( i = 0; i < k; i++)
	{
	  printf("\n%d",ans[i]);
	}
	getch();
}

