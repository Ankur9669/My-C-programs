#include<conio.h>
#include<stdio.h>
#define max 30
void main()
{
	clrscr();
	int i, j;
	int p[max], bt[max], wt[max], ta[max], n;
	printf("Enter the number: ");
	scanf("%d", &n);
	printf("Enter the process number: ");
	for (i = 0; i < n; i++)
	{
		scanf("%d", &p[i]);
	}
	printf("Enter the worst time: ");
	for (i = 0; i < n; i++)
	{
		scanf("%d", &wt[i]);
	}
	printf(" \nprocess number:");
	for (i = 0; i < n; i++)
	{
		printf("\n%d", p[i]);
	}
	printf(" \n burst time: ");
	for (i = 0; i < n; i++)
	{
		printf("\n%d", lwt[i]);
	}

	getch();
}